apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: Example PR Analysis with Claude

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

jobs:
  analyze-pr:
    steps:
      - name: Analyze PR
        id: analysis
        uses: swashbuck1r/claude-ci-tools@main
        with:
          anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          pr-number: ${{ cloudbees.scm.pull_request.number }}
          repository: ${{ cloudbees.scm.repository_full_name }}
          post-comment: "true"

      - name: Check Quality Score
        uses: docker://alpine:3.20
        shell: sh
        if: ${{ steps.analysis.outputs.quality-score < 7 }}
        run: |
          echo "⚠️ Quality score is below threshold: ${{ steps.analysis.outputs.quality-score }}/10"
          echo "Please review the analysis and address the issues"
          exit 1
